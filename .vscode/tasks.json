{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "command": "python build.py",
            "type": "shell",
            "options": {
                "cwd": "${cwd}/tools"
            }
        }
    ]
}